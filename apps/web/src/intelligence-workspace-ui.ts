export function renderKnowledgeIntelligenceWorkspace(): string {
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CERVEL — Knowledge Intelligence</title><style>
:root{color-scheme:dark;--bg:#080a0d;--panel:#0e1218;--panel2:#131922;--line:#222b37;--text:#f5f7f8;--muted:#8e99a8;--accent:#d8ff66;--warn:#ffc86b;--danger:#ff8585;--ok:#78e6a3}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 75% -20%,#172112 0,transparent 32%),var(--bg);color:var(--text);font:14px Inter,ui-sans-serif,system-ui,-apple-system,sans-serif}button,input,textarea{font:inherit}.app{display:grid;grid-template-columns:250px minmax(0,1fr);min-height:100vh}.side{border-right:1px solid var(--line);background:#0a0d12;padding:20px 14px;position:sticky;top:0;height:100vh}.brand{font-size:21px;font-weight:900;letter-spacing:.16em;padding:5px 10px 18px}.brand b{color:var(--accent)}.scope{margin:0 10px 22px;color:var(--muted);font-size:12px}.navgroup{margin:20px 10px 7px;color:#596575;text-transform:uppercase;letter-spacing:.16em;font-size:10px}.nav button{width:100%;display:flex;align-items:center;gap:9px;text-align:left;border:0;background:transparent;color:var(--muted);padding:10px 11px;border-radius:9px;cursor:pointer}.nav button:hover,.nav button.active{background:#151b24;color:var(--text)}.dot{width:7px;height:7px;border-radius:50%;background:#546171}.nav button.active .dot{background:var(--accent);box-shadow:0 0 12px #d8ff6680}main{padding:28px 32px;max-width:1500px;width:100%;margin:auto}.top{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:22px}.eyebrow{font-size:10px;text-transform:uppercase;letter-spacing:.18em;color:var(--accent)}h1{font-size:30px;margin:6px 0 4px;letter-spacing:-.025em}.muted{color:var(--muted)}.ghost,.primary{border-radius:9px;padding:9px 13px;cursor:pointer;font-weight:700}.ghost{border:1px solid var(--line);color:var(--text);background:#11161e}.primary{border:0;background:var(--accent);color:#10140a}.metrics{display:grid;grid-template-columns:repeat(7,minmax(110px,1fr));gap:10px;margin-bottom:18px}.metric,.card{background:linear-gradient(180deg,#11161e,#0e131a);border:1px solid var(--line);border-radius:14px}.metric{padding:13px}.metric .n{font-size:23px;font-weight:800;margin-top:5px}.metric .l{font-size:11px;color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:12px}.card{padding:16px}.card h3{margin:6px 0 5px;font-size:15px}.pill{display:inline-flex;align-items:center;border:1px solid #2d3744;border-radius:999px;padding:3px 7px;font-size:10px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.pill.ok{color:var(--ok)}.pill.warn{color:var(--warn)}.pill.bad{color:var(--danger)}.list{display:flex;flex-direction:column;gap:8px}.item{padding:12px 0;border-bottom:1px solid #1d2530}.item:last-child{border-bottom:0}.meta{font-size:11px;color:var(--muted);margin-top:7px}.row{display:flex;gap:9px;align-items:center}.spread{justify-content:space-between}.bar{height:6px;background:#222a35;border-radius:99px;overflow:hidden}.bar i{display:block;height:100%;background:var(--accent)}.askbox{max-width:1000px;margin:4vh auto}.askbox textarea{width:100%;min-height:155px;background:#0b1016;border:1px solid var(--line);border-radius:13px;padding:15px;color:var(--text);resize:vertical}.answer{white-space:pre-wrap;line-height:1.6;margin-top:12px}.empty{padding:38px;text-align:center;color:var(--muted)}.timeline{border-left:1px solid #283240;margin-left:8px;padding-left:18px}.timeline .item{position:relative}.timeline .item:before{content:'';position:absolute;width:8px;height:8px;border-radius:50%;background:var(--accent);left:-23px;top:17px}.healthring{width:120px;height:120px;border-radius:50%;display:grid;place-items:center;margin:auto;background:conic-gradient(var(--accent) calc(var(--score)*1%),#202833 0);position:relative}.healthring:after{content:'';position:absolute;inset:9px;border-radius:50%;background:#10151c}.healthring strong{position:relative;z-index:2;font-size:27px}.graph{min-height:520px;position:relative;overflow:hidden}.node{position:absolute;padding:7px 10px;border:1px solid #354253;background:#121923;border-radius:999px;max-width:175px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.field{background:#0c1117;border:1px solid var(--line);border-radius:9px;padding:9px;color:var(--text)}@media(max-width:1050px){.metrics{grid-template-columns:repeat(3,1fr)}}@media(max-width:760px){.app{grid-template-columns:1fr}.side{height:auto;position:relative;border-right:0;border-bottom:1px solid var(--line)}.nav{display:flex;overflow:auto}.navgroup,.scope{display:none}.nav button{white-space:nowrap}.metrics{grid-template-columns:repeat(2,1fr)}main{padding:20px 14px}}
</style></head><body><div id="root"></div><script>
const state={view:'overview',boot:null,intel:null};
const names={overview:'Intelligence Home',graph:'Knowledge Graph',timeline:'Timeline',sources:'Sources',changes:'Changes',claims:'Claims',decisions:'Decisions',contradictions:'Contradictions',health:'Knowledge Health',agents:'Agents',ask:'Ask CERVEL'};
const api=async(path,opts={})=>{const r=await fetch(path,{credentials:'same-origin',headers:{'content-type':'application/json',...(opts.headers||{})},...opts});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'REQUEST_FAILED');return d};
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
const fmt=d=>d?new Date(d).toLocaleString():'—';
function shell(content){const nav=(v)=>'<button data-view="'+v+'" class="'+(state.view===v?'active':'')+'"><span class="dot"></span>'+names[v]+'</button>';return '<div class="app"><aside class="side"><div class="brand">CERVEL<b>.</b></div><div class="scope">'+esc(state.boot?.session?.workspace_name||'Sovereign Knowledge')+'</div><div class="nav"><div class="navgroup">Intelligence</div>'+['overview','graph','timeline','changes','claims','decisions','contradictions'].map(nav).join('')+'<div class="navgroup">Operations</div>'+['sources','health','agents'].map(nav).join('')+'<div class="navgroup">Reasoning</div>'+nav('ask')+'</div></aside><main><div class="top"><div><div class="eyebrow">Knowledge Intelligence Workspace</div><h1>'+names[state.view]+'</h1><div class="muted">Traceable intelligence across the living knowledge system.</div></div><button id="logout" class="ghost">Sign out</button></div>'+content+'</main></div>'}
function metrics(){const o=state.intel.overview;return '<div class="metrics">'+[['Objects',o.objects],['Claims',o.claims],['Sources',o.sources],['Changes',state.intel.changes.length],['Contradictions',o.contradictions],['Agents',o.agents],['Health',o.health_score+'%']].map(x=>'<div class="metric"><div class="l">'+x[0]+'</div><div class="n">'+x[1]+'</div></div>').join('')+'</div>'}
function empty(label){return '<div class="card empty">No '+label+' yet.</div>'}
async function render(){let c=metrics();const d=state.intel;
if(state.view==='overview'){const recent=d.timeline.slice(0,6);c+='<div class="grid"><section class="card"><span class="pill">Live knowledge</span><h3>What changed recently</h3><div class="list">'+(recent.length?recent.map(eventItem).join(''):'<div class="empty">No recent knowledge events.</div>')+'</div></section><section class="card"><span class="pill">System signal</span><h3>Knowledge health</h3><div class="healthring" style="--score:'+d.overview.health_score+'"><strong>'+d.overview.health_score+'</strong></div><div class="meta" style="text-align:center">'+d.overview.open_health_issues+' open health issues · '+d.overview.contradictions+' contradictions</div></section><section class="card"><span class="pill">Proactive layer</span><h3>Active agents</h3><div class="list">'+(d.agents.length?d.agents.slice(0,6).map(agentItem).join(''):'<div class="empty">No agents configured.</div>')+'</div></section></div>'}
if(state.view==='timeline')c+=d.timeline.length?'<div class="card timeline">'+d.timeline.map(eventItem).join('')+'</div>':empty('timeline events');
if(state.view==='changes')c+=d.changes.length?'<div class="grid">'+d.changes.map(x=>'<article class="card"><span class="pill">'+esc(x.diff_kind)+'</span><h3>'+esc(x.title)+'</h3><div>'+esc(x.summary||'Semantic change detected')+'</div><div class="meta">v'+esc(x.previous_version||'?')+' → v'+esc(x.current_version)+' · +'+x.added_count+' −'+x.removed_count+' ~'+x.modified_count+' · '+Math.round(Number(x.confidence||0)*100)+'%</div><div class="meta">'+fmt(x.created_at)+'</div></article>').join('')+'</div>':empty('knowledge changes');
if(state.view==='claims')c+=d.claims.length?'<div class="grid">'+d.claims.map(x=>'<article class="card"><span class="pill '+(x.temporal_status==='current'?'ok':'warn')+'">'+esc(x.epistemic_status)+'</span><h3>'+esc(x.subject_label)+'</h3><div>'+esc(x.semantic_predicate||x.predicate)+'</div><div class="meta">'+esc(x.temporal_status)+' · '+Math.round(Number(x.confidence||0)*100)+'% confidence · '+fmt(x.created_at)+'</div></article>').join('')+'</div>':empty('claims');
if(state.view==='decisions')c+=d.decisions.length?'<div class="grid">'+d.decisions.map(x=>'<article class="card"><span class="pill">Decision</span><h3>'+esc(x.title)+'</h3><div class="muted">'+esc(x.summary||'No decision summary yet.')+'</div><div class="meta">'+x.events.length+' change events · updated '+fmt(x.updated_at)+'</div></article>').join('')+'</div>':empty('decision objects');
if(state.view==='contradictions')c+=d.contradictions.length?'<div class="grid">'+d.contradictions.map(x=>'<article class="card"><span class="pill bad">'+esc(x.conflict_type)+'</span><h3>'+esc(x.claim_a_predicate)+' ↔ '+esc(x.claim_b_predicate)+'</h3><div class="meta">'+Math.round(Number(x.confidence||0)*100)+'% conflict confidence · '+fmt(x.created_at)+'</div></article>').join('')+'</div>':empty('contradictions');
if(state.view==='sources')c+=d.sources.length?'<div class="grid">'+d.sources.map(x=>'<article class="card"><div class="row spread"><span class="pill '+(x.status==='connected'?'ok':'bad')+'">'+esc(x.status)+'</span><span class="muted">'+esc(x.provider)+'</span></div><h3>'+esc(x.account_email||x.provider)+'</h3><div>'+x.watched_count+' watched · '+x.fresh_count+' fresh · '+x.unhealthy_count+' unhealthy</div><div class="meta">Last success '+fmt(x.last_success_at)+'</div></article>').join('')+'</div>':empty('connected sources');
if(state.view==='health'){c+='<div class="grid"><section class="card"><div class="healthring" style="--score:'+d.overview.health_score+'"><strong>'+d.overview.health_score+'</strong></div><div class="meta" style="text-align:center">Knowledge Health Score</div></section><section class="card"><h3>Open issues</h3><div class="list">'+(d.health.length?d.health.map(x=>'<div class="item"><span class="pill '+(x.severity==='critical'?'bad':x.severity==='warning'?'warn':'')+'">'+esc(x.severity)+'</span><h3>'+esc(x.title)+'</h3><div class="muted">'+esc(x.message)+'</div><div class="meta">'+fmt(x.created_at)+(x.resolved_at?' · resolved':'')+'</div></div>').join(''):'<div class="empty">No health issues.</div>')+'</div></section></div>'}
if(state.view==='agents')c+=d.agents.length?'<div class="grid">'+d.agents.map(x=>'<article class="card"><div class="row spread"><span class="pill '+(x.enabled?'ok':'bad')+'">'+(x.enabled?'active':'disabled')+'</span><span class="muted">'+esc(x.kind)+'</span></div><h3>'+esc(x.name)+'</h3><div class="muted">'+esc(x.description||x.provider||'CERVEL agent')+'</div><div class="meta">'+x.observation_count+' observations · '+x.subscription_count+' subscriptions</div><div class="meta">'+(x.permissions||[]).map(esc).join(' · ')+'</div></article>').join('')+'</div>':empty('agents');
if(state.view==='graph'){const g=await api('/v1/workspace/graph');c+='<section class="card"><div class="row spread"><div class="muted">'+g.nodes.length+' entities · '+g.edges.length+' relationships</div><span class="pill">Semantic graph</span></div><div class="graph">'+g.nodes.slice(0,60).map((n,i)=>'<div class="node" style="left:'+(3+(i*29)%85)+'%;top:'+(38+(i*67)%430)+'px">'+esc(n.canonical_name)+'</div>').join('')+'</div></section>'}
if(state.view==='ask'){c+='<div class="askbox"><section class="card"><textarea id="q" placeholder="Ask CERVEL what changed, what conflicts, what is stale, why a decision moved, or what the knowledge currently supports..."></textarea><div class="row" style="margin-top:10px"><button id="ask" class="primary">Ask CERVEL</button><span class="muted">CCP → graph → temporal reasoning → cited answer</span></div></section><section id="answer" class="card answer">Your traceable answer will appear here.</section></div>'}
document.getElementById('root').innerHTML=shell(c);wire()}
function eventItem(x){return '<div class="item"><div class="row spread"><span class="pill">'+esc(x.event_type)+'</span><span class="muted">'+Math.round(Number(x.confidence||0)*100)+'%</span></div><h3>'+esc(x.summary)+'</h3><div class="meta">'+esc(x.subject_type)+' · '+x.impacts.length+' downstream impacts · '+fmt(x.observed_at)+'</div></div>'}
function agentItem(x){return '<div class="item"><div class="row spread"><strong>'+esc(x.name)+'</strong><span class="pill '+(x.enabled?'ok':'bad')+'">'+(x.enabled?'active':'off')+'</span></div><div class="meta">'+x.observation_count+' observations · '+x.subscription_count+' subscriptions</div></div>'}
function wire(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render()});document.getElementById('logout').onclick=async()=>{await api('/v1/session',{method:'DELETE'}).catch(()=>{});location.reload()};const ask=document.getElementById('ask');if(ask)ask.onclick=async()=>{answer.textContent='Reasoning over the knowledge graph…';try{const r=await api('/v1/workspace/ask',{method:'POST',body:JSON.stringify({query:q.value})});answer.innerHTML='<strong>'+esc(r.answer)+'</strong><div class="meta">Confidence '+Math.round(Number(r.semantic?.confidence?.final||0)*100)+'% · <a style="color:var(--accent)" href="/trace/'+esc(r.id)+'">Open trace</a></div>'}catch(e){answer.textContent=e.message}}}
async function boot(){try{const status=await api('/v1/auth/status');if(!status.authenticated){location.href='/workspace/alpha';return}state.boot=await api('/v1/workspace');state.intel=await api('/v1/workspace/intelligence');render()}catch(e){document.getElementById('root').innerHTML='<div class="empty">'+esc(e.message)+'</div>'}}
boot();
</script></body></html>`;
}
