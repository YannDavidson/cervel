# Agent Runtime acceptance criteria

PR #12 is acceptable when an external-style agent principal can be granted one Workspace, persist an observation and provenance-bearing claim, consume a CCP, and receive the relevant Knowledge Event plus Watch alert without seeing another Workspace.

The same final head must pass the existing CERVEL regression matrix and the Agent Knowledge Runtime integration lane before the PR leaves draft.
