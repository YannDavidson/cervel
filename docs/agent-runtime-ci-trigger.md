# CI trigger

Opening PR #12 against `main` will trigger the new Agent Knowledge Runtime Integration workflow and the existing workflows whose path filters include `apps/api/**` and `db/migrations/**`.
