# Dedicated validation target

The new CI lane's non-negotiable behavioral proof is: one correctly granted agent can create durable observation + Claim provenance, while the exact same principal is denied in a second Workspace with no agent grant.
