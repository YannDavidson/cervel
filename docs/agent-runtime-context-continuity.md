# Context continuity

Durable observations and claims let an agent preserve knowledge across model sessions, while CCP assembly reconstructs task-specific context at use time. The agent therefore does not need to replay an entire chat transcript to recover state.

Continuity is grounded in CERVEL objects, claims, provenance, permissions, and current knowledge rather than a provider's opaque conversation memory.
