# Last implementation note

The runtime is intentionally small enough that every authorization decision can be manually audited. Preserve that property as protocol adapters and agent-team features are added later.
