# Durable agent memory

An observation is an attributable memory record, not automatically a canonical fact. When an agent also makes a structured assertion, CERVEL writes a normal claim with confidence and provenance and defaults it to `claimed` epistemic status.

This separation lets agents remember operational observations while keeping the knowledge graph's truth-status machinery explicit and reviewable.
