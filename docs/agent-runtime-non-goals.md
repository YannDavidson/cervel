# PR #12 non-goals

PR #12 does not introduce autonomous tool execution, unrestricted agent-to-agent delegation, model-provider-specific memory, background infinite polling, or a new authorization system. It also does not automatically promote agent assertions to verified knowledge.

Those capabilities can build on this runtime after the identity, memory, context, provenance, and signal contracts are stable.
