# Agent Runtime open questions after v0.1

Questions intentionally deferred from PR #12: whether observation memory should support consolidation/decay; when agent observations should auto-promote to Claims; how human review affects agent Claim epistemic status; how delegated agent teams inherit/narrow grants; how push delivery retries interact with receipts; how short-lived agent credentials are issued/rotated; how quotas are enforced; and how MCP/A2A protocol semantics map to CERVEL provenance.

None of these require changing the core v0.1 decision that a CERVEL principal + exact Workspace grant is the authorization boundary.
