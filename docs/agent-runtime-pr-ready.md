# Ready-for-Review criteria

Ready only when: exact final head is mergeable; all triggered legacy workflows succeed; Agent Knowledge Runtime Integration succeeds; no unresolved real runtime/migration failure remains; final authorization/provenance/signal diff review finds no blocker.
