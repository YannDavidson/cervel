# Agent Runtime product contract

CERVEL should let an authorized AI agent return tomorrow, recover durable Workspace knowledge, obtain current context, record new observations with provenance, and react when relevant knowledge changes.

The agent must never need blanket database access or vendor-specific memory. CERVEL remains the sovereign knowledge layer; the model or agent framework remains replaceable.
