# Release intent

PR #12 is intended to ship as the first stable internal Agent Knowledge Runtime contract in CERVEL v0.1. It is not yet a public internet-facing agent API. Production exposure should follow the later agent-auth/protocol milestone with short-lived credentials, rate controls and transport-specific hardening.
