# Release message draft

CERVEL now has an Agent Knowledge Runtime: internal and external AI agents can operate as first-class, permission-scoped CERVEL principals; recover governed Context Packages; persist durable observations and provenance-aware Claims; and listen to Knowledge Events / CERVEL Watch signals across sessions. The memory belongs to CERVEL, not the model provider.
