# Final review target

The highest-risk code in PR #12 is not the additive schema; it is authorization around signal union queries and administrative grant creation. Review those paths manually even when CI is green.
