# Agent Runtime roadmap

After v0.1, the same contracts can support signed machine credentials, OAuth/OIDC agent exchange, MCP and A2A adapters, queue/webhook delivery, agent teams with delegated grants, human approval gates, budget/rate policies, and richer memory lifecycle controls.

Those are follow-on protocol and orchestration layers; PR #12 establishes the durable knowledge-runtime substrate they depend on.
