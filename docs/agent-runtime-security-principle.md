# Security principle

**Agent capability is never agent authority.**

An agent may advertise that it can research, code, browse, reason or write memory. CERVEL treats those as implementation capabilities only. Authority exists solely through the authenticated CERVEL principal and explicit Workspace grant for the requested operation.
