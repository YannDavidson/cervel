# Agent Runtime success criteria

A successful v0.1 makes CERVEL usable as persistent, permission-aware knowledge infrastructure by an AI agent without granting that agent blanket Node access or creating a separate memory silo.

The minimum proof is: scoped identity, durable write, explicit claim provenance, CCP read, cross-tenant denial, Event/Impact filtering, Watch authorization, durable signal delivery, and acknowledgement.
