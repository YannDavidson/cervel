# System promise

CERVEL should be able to replace the AI agent without losing the agent's authorized institutional memory, and replace the AI provider without rewriting the knowledge graph.

PR #12 establishes the primitives required for that promise: provider-neutral agent identity, explicit Workspace authority, durable observations/Claims, reproducible CCP context, and CERVEL-native change signals.
