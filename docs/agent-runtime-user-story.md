# Core user story

As a builder, I can connect an AI agent to CERVEL, grant it only one Workspace and the minimum knowledge capabilities it needs, let it recover governed context, persist useful learning, and receive relevant future knowledge changes — without giving the model vendor ownership of my memory or access to unrelated tenant data.
