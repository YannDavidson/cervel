# Agent Knowledge Runtime vision

CERVEL should become the place where an organization's AI agents **remember what the organization knows**, not a database each agent copies into its own private memory.

A governed shared substrate makes agent replacement, specialization and collaboration possible while retaining provenance and tenant boundaries. PR #12 is the first runtime contract for that direction.
