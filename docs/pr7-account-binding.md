# Provider account binding

A source connection belongs to one CERVEL Node, Workspace, Principal, provider, and verified provider-account subject. Reconnecting the same external account in another Workspace creates a separate scoped connection record rather than sharing credentials across Workspace boundaries.
