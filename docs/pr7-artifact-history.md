# Artifact history

Because changed revisions create new artifact IDs/object keys, storage history is append-only for synchronized source bytes. The CKO's original primary artifact is not silently replaced; later snapshots remain linked to the same CKO.
