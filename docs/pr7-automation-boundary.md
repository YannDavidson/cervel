# Automation boundary

The automation dispatcher chooses due watches from persisted policy; callers cannot provide arbitrary connection IDs, Node IDs, Workspace IDs, or remote URLs to the internal due-sync route. This keeps scheduler authority narrow.
