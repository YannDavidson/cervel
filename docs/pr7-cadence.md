# Sync cadence

Watch cadence is configurable from 5 minutes through 7 days. `next_sync_at` is persisted so scheduler frequency and source frequency are decoupled. Resuming a paused source makes it immediately due.
