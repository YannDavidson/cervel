# Change history semantics

The latest source-document row is a pointer to current upstream content state; immutable artifacts and CKO version snapshots are the historical record. Updating the pointer therefore never destroys previous synchronized evidence.
