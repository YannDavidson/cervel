# Connection listing

The authenticated connection-list endpoint selects only ID, provider, account email, status, last error, token expiry timestamp, and lifecycle timestamps. Encrypted access/refresh token fields and provider-state internals are deliberately excluded.
