# Fail-closed behavior

Missing provider client configuration, missing encryption key, invalid/expired OAuth state, missing Workspace session, cross-scope watch/Library access, missing automation secret, missing refresh authorization, and oversized remote content all stop the operation rather than silently weakening the boundary.
