# Provider file-picker follow-up

A polished Sources UI should add provider-backed browse/search endpoints and file pickers, returning stable remote IDs and display metadata to the watch-creation route. Keep those endpoints read-only and scoped to the authenticated source connection.
