# Scope freeze

PR #7 freezes at provider authorization + selected-file synchronization + version/freshness/health backend contracts. File-picker UX, folder recursion, push webhooks, binary extraction, outbound health delivery, and scale queueing are follow-ups rather than scope creep for this rollout.
