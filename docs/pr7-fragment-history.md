# Fragment history

Each ingestible artifact revision produces its own fragments tied to that artifact. This preserves evidence from historical source representations. Retrieval/version-selection policy can evolve separately from synchronization without destroying prior fragments.
