# Knowledge Health boundary

Health rows never contain provider access/refresh credentials. User-facing messages use normalized CERVEL conditions rather than raw provider payloads, reducing accidental credential/PII leakage through operational error surfaces.
