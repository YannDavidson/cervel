# Initial synchronization

The first successful sync creates the CKO using the watched source's Workspace and Principal, registers the provider bytes as the original artifact, ingests/embeds supported content, records the stable source-document mapping, optionally routes the CKO into a Library, and marks the source fresh.
