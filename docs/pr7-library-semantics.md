# Library semantics

Library assignment is organizational routing, not source identity. A watched remote file retains one CKO even if its Library destination changes. This avoids duplicating knowledge objects merely because users reorganize their Workspace.
