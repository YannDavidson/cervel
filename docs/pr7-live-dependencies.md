# Live dependencies

The code path is complete without committing credentials, but real provider synchronization cannot be exercised until OAuth applications exist for Google, Dropbox, and Microsoft. Those external credentials plus a scheduler are the only intentional rollout dependencies after merge validation.
