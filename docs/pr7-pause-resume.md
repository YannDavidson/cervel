# Pause and resume

The watch update endpoint can disable or re-enable automatic synchronization without deleting source configuration or CERVEL knowledge. Pausing sets source state to `paused`; resuming returns it to `pending` when appropriate and schedules an immediate check.
