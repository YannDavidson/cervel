# Principle: upstream outages should not erase downstream utility

Provider downtime can stop freshness, but previously synchronized CERVEL artifacts/fragments remain available for retrieval. Health state tells the user the source is no longer current instead of making knowledge disappear unpredictably.
