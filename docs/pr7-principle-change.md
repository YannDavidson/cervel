# Principle: compare content, not provider bookkeeping

Providers differ in revision/version semantics. SHA-256 over synchronized bytes gives CERVEL one stable rule for deciding whether knowledge actually changed while still retaining provider metadata for diagnostics and optimization.
