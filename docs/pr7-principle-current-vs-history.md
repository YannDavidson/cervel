# Principle: current state and history are separate projections

`watched_sources` and `source_documents` answer what is current; `source_sync_runs`, artifacts, and CKO versions answer what happened before. Updating current state must not erase execution or evidence history.
