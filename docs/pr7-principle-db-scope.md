# Principle: tenant scope deserves database reinforcement

Application checks validate connection and Library scope, and composite foreign keys reinforce those relationships in PostgreSQL. Cross-Workspace connector references should be structurally invalid, not merely discouraged by route code.
