# Principle: acknowledgement is not recovery

A user marking a health item read should not resolve the underlying stale/error/authorization condition. Read and resolved timestamps therefore remain separate.
