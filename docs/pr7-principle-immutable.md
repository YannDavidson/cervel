# Principle: synchronized evidence artifacts are immutable

Every changed remote representation receives a new artifact ID/object key. CERVEL never mutates the bytes of an existing source artifact to make it look current.
