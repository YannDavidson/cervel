# Principle: request the minimum upstream authority

The first connector release needs account identity, file metadata/content read, and offline refresh—not provider write permissions. OAuth scopes are chosen accordingly.
