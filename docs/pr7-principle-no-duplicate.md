# Principle: metadata churn should not duplicate knowledge

Provider timestamps/revisions can change without meaningful content change. The hash gate ensures such churn produces an unchanged sync record rather than duplicate CKO artifacts/embeddings.
