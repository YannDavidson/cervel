# Principle: automation must preserve provenance

Connector content enters through the existing artifact/ingestion pipeline so captured/extracted provenance continues to exist. Background synchronization should be at least as traceable as manual capture.
