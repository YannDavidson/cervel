# Principle: provider account identity must be verified

CERVEL binds a connection to a provider-asserted account subject from a verified ID token or authenticated account endpoint. Opaque token strings are credentials, not identity claims.
