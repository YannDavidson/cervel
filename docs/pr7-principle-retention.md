# Principle: upstream disconnect does not imply knowledge deletion

Authorization lifecycle and CERVEL knowledge retention are intentionally separate. Losing or revoking upstream access stops future refresh but does not silently erase previously synchronized knowledge/evidence.
