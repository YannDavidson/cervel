# Principle: reuse the existing knowledge pipeline

Connectors call the same CKO, artifact, ingestion, embedding, Library, retrieval, and provenance contracts used elsewhere in CERVEL. Source automation should increase how knowledge arrives and stays current, not create a parallel knowledge architecture.
