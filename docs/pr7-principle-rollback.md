# Principle: disable automation without invalidating synchronized knowledge

Connector rollout can be rolled back operationally by stopping scheduler/provider entrypoints while leaving additive schema and previously synchronized CKOs intact. External refresh capability and downstream knowledge utility are separable.
