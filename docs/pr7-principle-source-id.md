# Principle: remote source IDs are opaque

CERVEL stores provider remote IDs without assuming cross-provider format or meaning. Provider adapters alone interpret how an ID is used in their fixed API endpoints.
