# Principle: account connection is not consent to ingest everything

PR #7 separates provider-account authorization from watched-source selection. CERVEL synchronizes explicitly configured remote sources rather than interpreting account connection as permission to mirror the entire drive.
