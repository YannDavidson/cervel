# Principle: source acquisition needs size limits

Remote provider content is untrusted workload input even when the user authorized the account. A configurable byte cap prevents a selected file from causing unbounded memory/storage/ingestion work in the Alpha runtime.
