# Principle: encrypted credentials are still credentials

Ciphertext is not safe product metadata. Connection APIs deliberately omit encrypted token columns rather than relying on encryption as justification to serialize them to clients.
