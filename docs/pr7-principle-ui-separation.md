# Principle: stabilize backend contracts before polishing Sources UI

PR #7 establishes authorization, synchronization, versioning, freshness, and health APIs first. A dedicated Sources UI can then consume stable contracts without forcing security/data-model changes during visual iteration.
