# Principle: synchronize by versioning, not replacement

When upstream evidence changes, CERVEL should preserve what it previously knew and add the new representation. Stable CKO identity plus immutable artifacts/version snapshots gives automation memory instead of silent overwrite behavior.
