# Provider rollout

Provider implementations are independent behind the common connector contract. Roll them out progressively rather than enabling all three simultaneously in production. Google Drive is a practical first smoke target because native Google documents can be exported directly to text for the existing Alpha ingestion pipeline.
