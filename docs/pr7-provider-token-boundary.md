# Provider token boundary

OAuth tokens are used only for provider API calls and refresh, never as CERVEL sessions or principal identifiers. CERVEL authorization continues to use its own Workspace session and Principal model.
