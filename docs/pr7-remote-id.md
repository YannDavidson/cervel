# Remote IDs

Remote IDs are treated as opaque provider locators and encoded when placed into provider URL paths. They are never interpreted as filesystem paths by CERVEL storage and never used to construct local object keys.
