# Rollback

If connector runtime must be disabled after deployment, stop the scheduler and hide/disable provider connection entrypoints first. The additive migrations can remain in place; synchronized CKOs/artifacts continue to work through existing CERVEL retrieval/reasoning even while external refresh is disabled.
