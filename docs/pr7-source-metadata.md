# Source metadata

Watched-source and source-document rows retain only synchronization/product metadata needed for cadence, provider diagnostics, display, content hash, remote modification/version, and routing. Provider authorization credentials stay isolated in `source_connections`.
