# Source naming

The watch stores a human-readable source name for product display, but successful synchronization refreshes the CKO title from provider metadata. The provider remote ID remains the stable synchronization locator.
