# State layers

Connection state answers whether CERVEL can access an account. Watch state answers whether a selected source is current/paused/error. Sync-run state records what happened on each execution. Knowledge Health answers what requires user/operator attention. Keeping these layers separate prevents one overloaded status field from becoming ambiguous.
