# Test boundary

Repository CI validates CERVEL-controlled logic without third-party credentials. Provider OAuth/download semantics require live smoke testing in a configured environment. This separation prevents secret exposure while still making schema, type, state, and regression failures merge-blocking.
