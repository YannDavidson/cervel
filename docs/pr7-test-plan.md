# Test plan

Automated: build, Jest, clean migrations, schema probes, freshness transition, and every pre-existing CERVEL integration lane.

Live deployment: OAuth start/callback/account binding, initial sync, changed sync, unchanged sync, Library routing, manual sync, pause/resume, revoked-token health, and cross-Workspace denial for each provider.
