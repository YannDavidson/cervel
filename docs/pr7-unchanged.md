# Unchanged synchronization

When SHA-256 matches the latest source-document hash, CERVEL updates successful-check/freshness metadata and records an unchanged sync run. It does not create an artifact, fragments, embeddings, or a CKO version.
