export const MOBILE_LIFE_MEMORY_PROTOCOL="cervel-mobile-life-memory/v0.1" as const;
export type LifeMemoryView="today"|"timeline"|"people"|"places"|"goals"|"moments";
export type LifeMemoryObject={id?:string;cko_id?:string;title?:string;summary?:string;mega_tab?:string;subtab?:string;branch_visibility?:string;branch_sensitivity?:string;cko_updated_at?:string;updated_at?:string;[key:string]:unknown};
const viewBranches:Record<LifeMemoryView,readonly string[]>={today:["memory","personal-knowledge","relationships-family","travel","goals","projects"],timeline:["memory"],people:["relationships-family","memory"],places:["travel","home","memory"],goals:["goals","projects"],moments:["memory","travel","relationships-family","hobbies"]};
const time=(v:LifeMemoryObject)=>Date.parse(String(v.cko_updated_at??v.updated_at??0))||0;
export function buildLifeMemoryView(objects:LifeMemoryObject[],view:LifeMemoryView="today",limit=40){const allowed=new Set(viewBranches[view]);const rows=objects.filter(v=>allowed.has(String(v.mega_tab??""))&&v.branch_visibility!=="sealed").sort((a,b)=>time(b)-time(a)).slice(0,Math.max(1,Math.min(100,limit)));return{protocol:MOBILE_LIFE_MEMORY_PROTOCOL,view,objects:rows,counts:{total:rows.length,memory:rows.filter(v=>v.mega_tab==="memory").length,people:rows.filter(v=>v.mega_tab==="relationships-family").length,places:rows.filter(v=>v.mega_tab==="travel"||v.mega_tab==="home").length,goals:rows.filter(v=>v.mega_tab==="goals"||v.mega_tab==="projects").length}};}
export const LIFE_MEMORY_VIEWS=(Object.keys(viewBranches) as LifeMemoryView[]).map(key=>({key,branches:[...viewBranches[key]]}));
