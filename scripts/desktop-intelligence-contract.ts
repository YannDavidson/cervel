import{readFileSync}from'node:fs';const must=(ok:boolean,msg:string)=>{if(!ok)throw new Error(msg)};const rust=readFileSync('apps/desktop-tauri/src-tauri/src/lib.rs','utf8'),html=readFileSync('apps/desktop-tauri/ui/index.html','utf8'),js=readFileSync('apps/desktop-tauri/ui/intelligence.js','utf8'),conf=JSON.parse(readFileSync('apps/desktop-tauri/src-tauri/tauri.conf.json','utf8'));for(const command of ['ask_cervel','answer_trace','intelligence_status','intelligence_receipts'])must(rust.includes(command),`missing ${command}`);must(rust.includes('/v1/reason'),'Cortex must use canonical reasoning route');must(rust.includes('/v1/answers/{id}/trace'),'Trace must use canonical protected answer lineage');must(rust.includes('/v1/intelligence/providers')&&rust.includes('/v1/intelligence/receipts'),'Desktop must surface Intelligence Gateway state and receipts');must(rust.includes('x-cervel-local-token')&&rust.includes('x-cervel-principal-id'),'native intelligence bridge must preserve Local Node auth');must(!conf.app.security.csp.includes('127.0.0.1')&&!conf.app.security.csp.includes('localhost:*'),'renderer must not directly access Local Node');for(const id of ['cortex-query','ask-cervel','cortex-answer','provider-summary','trace-current','trace-receipts'])must(html.includes(`id="${id}"`),`missing ${id}`);must(html.includes('Authorized context is assembled before any model is called.'),'authorization-first product boundary required');must(html.includes('Knowledge stays here.<br>Reasoning may happen there.'),'sovereignty disclosure required');must(js.includes('intelligenceEsc'),'intelligence renderer output must be escaped');must(js.includes('metaKey')&&js.includes("key==='Enter'"),'Cmd/Ctrl+Enter Ask CERVEL required');must(js.includes('externalDisclosure')&&js.includes('adapter?.provider'),'UI must distinguish actual execution from disclosure route');must(js.includes('Authorized context crossed the local boundary'),'external disclosure must be stated independently of final provider');must(js.includes('intelligence_receipts'),'Trace must surface routing receipts');must(js.includes('offline_only')&&js.includes('network_allowed'),'Desktop must show active network-routing policy');must(!js.includes("fetch('http://127.0.0.1")&&!js.includes('OPENAI_API_KEY'),'renderer must not bypass Gateway or receive provider secrets');console.log('CERVEL Desktop Cortex + Trace contract: OK');