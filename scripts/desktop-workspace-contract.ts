import{readFileSync}from'node:fs';const must=(v:boolean,m:string)=>{if(!v)throw new Error(m)};const rust=readFileSync('apps/desktop-tauri/src-tauri/src/lib.rs','utf8'),html=readFileSync('apps/desktop-tauri/ui/index.html','utf8'),js=readFileSync('apps/desktop-tauri/ui/workspace.js','utf8'),cli=readFileSync('apps/local-node/src/cli.ts','utf8');for(const command of ['vault_objects','capture_note','capture_files'])must(rust.includes(command),`missing ${command}`);must(rust.includes('MAX_DESKTOP_FILE_BYTES'),'file size boundary required');must(rust.includes('/v1/objects')&&rust.includes('/artifacts'),'Desktop capture must create canonical CKO + artifacts through Local Node');must(rust.includes('/v1/local/objects'),'Vault workspace must read canonical Local Node objects');must(!rust.includes('desktop-workspace.json'),'parallel Desktop knowledge store is forbidden');must(!rust.includes('artifacts/desktop'),'Desktop must not bypass canonical artifact storage');must(rust.includes('x-cervel-local-token')&&rust.includes('x-cervel-principal-id'),'native bridge must preserve Local Node auth boundary');must(cli.includes('desktop-session.json')&&cli.includes('{mode:0o600}'),'Desktop session token must be owner-only');for(const id of ['vault-items','vault-search','note-title','note-body','drop-zone','capture-receipt'])must(html.includes(`id="${id}"`),`missing ${id}`);must(js.includes('escapeHtml'),'Vault output must be escaped');must(js.includes('metaKey')&&js.includes("key==='Enter'"),'Cmd+Enter capture required');must(html.includes('Knowledge stays here. Reasoning may happen there.'),'sovereignty boundary required');console.log('CERVEL Desktop Capture + Vault Workspace contract: OK');