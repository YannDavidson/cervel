import{readFileSync}from"node:fs";const read=(p:string)=>readFileSync(p,"utf8"),must=(v:boolean,m:string)=>{if(!v)throw new Error(m)};const protocol=read("packages/mobile-capture/src/share.ts"),ios=read("apps/mobile/native/ios/ShareExtension/ShareViewController.swift"),android=read("apps/mobile/native/android/ShareReceiverActivity.kt"),docs=read("docs/UNIVERSAL_SHARE_TO_CERVEL.md");for(const token of ["ios_share_extension","android_sharesheet","safari_handoff","untrusted_captured_content","never_execute","UNIVERSAL_SHARE_TOO_LARGE"])must(protocol.includes(token),`share protocol missing ${token}`);must(ios.includes("group.ai.cervel.capture")&&ios.includes("cervel://share/import"),"iOS share handoff boundary missing");must(android.includes("Intent.ACTION_SEND")&&android.includes("cervel://share/import"),"Android sharesheet boundary missing");must(docs.includes("encrypted outbox")&&docs.includes("canonical CKO/artifact/provenance"),"canonical share pipeline must be documented");must(!docs.includes("share-memory.db"),"share must not create parallel knowledge store");console.log("CERVEL Universal Share contract: OK");
