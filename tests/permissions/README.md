# Permission acceptance tests

Required:
- denied principal cannot retrieve fragment via FTS
- denied principal cannot retrieve fragment via vector index
- denied principal cannot reach fragment by graph expansion
- denied principal cannot retrieve claim evidence
- model adapter cannot expand CCP authorization scope
